#include "main.h"

/**
 * swap_int - store 2var value revrese
 *@a: pointer for a var
 *@b: pointer for b var
 * Return: nothing
 */

void swap_int(int *a, int *b)
{
int g;

g = *a;
*a = *b;
*b = f;
}
