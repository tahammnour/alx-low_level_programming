#include "main.h"

/**
 * _isupper - check char upercase
 * @c: the param which is char c
 * Return: 1 if it upper and 0 for lower in main.c
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
